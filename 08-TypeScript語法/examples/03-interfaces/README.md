# 範例 3：interface 與 type（可複製專案）

此資料夾為 [範例 3](../../範例3-interface與type.md) 的網頁練習環境。**與 tsx-cli 不必相同**：以「**閱讀清單書架**」呈現 `interface`、`readonly`、選用屬性與 `extends`。

## 檔案說明

| 檔案 | 說明 |
|------|------|
| `src/practice-03-interfaces.ts` | 書籍資料結構與卡片渲染 |
| `src/style.css` | 酒紅書櫃風格 |

## 執行

```bash
cd 此資料夾
npm install
npm run dev
```
