# 範例 2：函式與型別（可複製專案）

此資料夾為 [範例 2](../../範例2-函式與型別.md) 的網頁練習環境。**與 tsx-cli 的 `02-functions.ts` 不必相同**：以「**輕食結帳／收據預覽**」呈現回傳型別、選用參數、預設幣別與 `void` 記錄等觀念。

## 檔案說明

| 檔案 | 說明 |
|------|------|
| `src/practice-02-functions.ts` | 函式與表單互動 |
| `src/style.css` | 編輯風橄欖綠主題 |

## 執行

```bash
cd 此資料夾
npm install
npm run dev
```
