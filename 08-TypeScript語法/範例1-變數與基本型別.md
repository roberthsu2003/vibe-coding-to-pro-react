# 範例 1：變數與基本型別

## 目標

學會使用 `let` / `const` 搭配型別註解，並熟悉 `string`、`number`、`boolean` 等基本型別，以及**型別推斷**與**明確註解**的取捨。

---

## 前置條件

- 建議已完成 [範例 0：建立練習專案](範例0-建立練習專案.md)（或使用任一 `.ts` 檔練習）

> **與 03 章節的關係**：[03 - TypeScript 配置](../03-TypeScript配置/範例3-基本型別.md) 已介紹過基本型別宣告；本範例補上 `let`/`const`、推斷與註解的完整脈絡，方便獨立閱讀。

---

## 步驟 1：`let` 與 `const`

| 關鍵字 | 說明 |
|--------|------|
| **`const`** | 綁定後**不可重新指定**另一個值（常數綁定）。物件／陣列的「內容」仍可修改（除非用其他方式凍結）。 |
| **`let`** | 可重新指定，作用域為**區塊**（`{ }` 內）。 |

```typescript
const pi = 3.14
// pi = 3  // ❌ 錯誤：const 不可重新賦值

let score = 0
score = 85  // ✅ 可以改變 score 的值
```

在 TypeScript 中，**預設優先使用 `const`**，需要重複賦值時再用 `let`。

---

## 步驟 2：型別註解（`: 型別`）

在變數名稱後加上 `: 型別`，告訴編譯器這個變數允許的值。

```typescript
const userName: string = '小明'
const age: number = 18
const passed: boolean = true
```

- **`string`**：字串  
- **`number`**：數字（整數、浮點數皆為 `number`）  
- **`boolean`**：`true` 或 `false`  

若賦值的型別不符，編輯器會立刻標錯：

```typescript
// const wrong: number = '一百'  // ❌ string 不可賦給 number
```

---

## 步驟 3：型別推斷

若**宣告時就給定值**，TypeScript 常能自動推斷型別，此時可省略註解：

```typescript
const title = 'Hello'   // 推斷為 string
const count = 10        // 推斷為 number
const ok = false        // 推斷為 boolean
```

**何時建議寫明型別？**

- 先宣告、晚一點才賦值  
- 希望限制「只能是某幾種值」時（與後續聯合型別、字面量型別搭配）  
- 團隊規範要求可讀性與 API 邊界清楚  

```typescript
let total: number
total = 0
total = total + 1
```

---

## 步驟 4：`bigint`（選讀）

當整數**超過 `number` 的安全整數範圍**，可使用 `bigint`：

```typescript
const big: bigint = 9007199254740993n
```

一般教學與前端專案仍以 `number` 為主；遇到金額、大整數運算再查 `bigint` 即可。

---

## 步驟 5：在練習專案中試作

若已依 [範例 0](範例0-建立練習專案.md) 建立 Vite 專案，可開啟 `src/main.ts`（或新增 `src/practice.ts` 並在 `main` 中 import），貼上並修改：

```typescript
const appName = 'TS 練習'
let visitCount: number = 0

function tick() {
  visitCount = visitCount + 1
  console.log(`${appName}：第 ${visitCount} 次`)
}

tick()
tick()
```

執行 `npm run dev` 並開啟瀏覽器主控台，應看到兩行 log。

---

## 重點整理

| 概念 | 說明 |
|------|------|
| `const` / `let` | 優先 `const`；需要重複賦值用 `let` |
| `: string` 等 | 明確型別註解，便於檢查與閱讀 |
| 型別推斷 | 有初始值時常可省略註解 |
| `string`、`number`、`boolean` | 最常用的三種基本型別 |

---

## 小練習

1. 宣告一個 `const` 字串變數表示你的暱稱，並用 `console.log` 印出。  
2. 宣告 `let score: number = 0`，再將 `score` 設為 `100`，確認無型別錯誤。  
3. 故意將 `boolean` 變數賦值為數字，觀察編輯器顯示的錯誤訊息。

---

## 完成

你已熟悉變數宣告與基本型別。接下來請繼續 [範例 2：函式與型別](範例2-函式與型別.md)。

---

[上一範例：範例 0 - 建立練習專案](範例0-建立練習專案.md) | [下一範例：範例 2 - 函式與型別](範例2-函式與型別.md)
