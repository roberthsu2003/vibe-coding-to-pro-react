# 範例 2：認識專案結構

## 目標

認識 Next.js App Router 專案的資料夾與檔案結構。

---

## 前置條件

- 已完成 [範例 0：建立專案](範例0-建立專案.md)

---

## 步驟 1：專案根目錄

```
examples/
├── src/
│   └── app/          ← 應用程式（App Router）
├── public/           ← 靜態資源
├── package.json
├── next.config.ts    ← Next.js 設定
└── tsconfig.json
```

---

## 步驟 2：app 資料夾（App Router）

```
src/app/
├── layout.tsx        ← 根佈局（共用 HTML、導覽）
├── page.tsx          ← 首頁（/）
├── globals.css       ← 全域樣式
└── favicon.ico
```

---

## 步驟 3：重要檔案說明

| 檔案 | 說明 |
|------|------|
| **layout.tsx** | 根佈局，包住所有頁面，可放導覽列 |
| **page.tsx** | 對應路由的頁面，`app/page.tsx` = `/` |

---

## 步驟 4：檔案即路由

在 `app` 下建立資料夾與 `page.tsx`，即自動成為路由：

| 路徑 | URL |
|------|-----|
| `app/page.tsx` | `/` |
| `app/about/page.tsx` | `/about` |
| `app/products/page.tsx` | `/products` |

---

## 完成

認識結構後，繼續 [範例 3：路由與頁面](範例3-路由與頁面.md)。

---

[上一範例：範例 1 - React 與 Next.js 的關係](範例1-React與Next.js的關係.md) | [下一範例：範例 3 - 路由與頁面](範例3-路由與頁面.md)
