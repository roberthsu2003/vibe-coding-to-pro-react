# 範例 4：資料載入

## 目標

學會在 Server Component 中直接 `async` 載入資料，無需 useEffect。

---

## 前置條件

- 已完成 [範例 3：路由與頁面](範例3-路由與頁面.md)

---

## 步驟 1：認識 Server Component

Next.js App Router 的元件**預設為 Server Component**，可：

- 直接使用 `async` 函式
- 在伺服器端執行，不暴露給客戶端
- 直接 `fetch` 或查詢資料庫

---

## 步驟 2：建立有資料載入的頁面

建立 `src/app/products/page.tsx`：

```tsx
async function getProducts() {
  // 模擬 API 或資料庫查詢
  return [
    { id: 1, name: "商品 A", price: 100 },
    { id: 2, name: "商品 B", price: 200 },
  ];
}

export default async function Products() {
  const products = await getProducts();

  return (
    <main className="p-8">
      <h1 className="text-2xl font-bold">商品列表</h1>
      <ul className="mt-4 space-y-2">
        {products.map((p) => (
          <li key={p.id}>
            {p.name} - ${p.price}
          </li>
        ))}
      </ul>
    </main>
  );
}
```

---

## 步驟 3：重點整理

- `export default async function`：頁面元件可為 async
- `await getProducts()`：在伺服器端執行，資料不會送到客戶端
- 無需 `useEffect`、`useState` 載入資料

---

## 步驟 4：驗證

1. 儲存檔案
2. 開啟 `http://localhost:3000/products`
3. 應看到商品列表

---

## 完成

學會資料載入後，繼續 [範例 5：Server Actions 表單處理](範例5-Server-Actions表單處理.md)。

---

[上一範例：範例 3 - 路由與頁面](範例3-路由與頁面.md) | [下一範例：範例 5 - Server Actions 表單處理](範例5-Server-Actions表單處理.md)
